/**
 * A dummy function as example for documentation generation
 * @param input String that wil be logged to the console
 * @returns Nothing, it just logs something
 */
export declare const someFunction: (input: string) => void;
